<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MediaStore.createWriteRequest" />
    <uses-feature android:name="android.hardware.camera"
        android:required="true" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.GCTI"
        tools:targetApi="31">

        <activity android:name=".abrirchamado.AbrirChamado" />
        <activity android:name=".abrirchamado.AbrirChamado2"/>
        <activity android:name=".abrirchamado.AbrirChamado3" />
        <activity android:name=".abrirchamado.ProtocoloActivity" />


        <activity android:name=".consult.ConsultTicketActivity" />
        <activity android:name=".consult.ResultadoConsultActivity" />
        <activity android:name=".ti.ITDepartmento" />
        <activity android:name=".ti.ITDepartmento2" />


        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>